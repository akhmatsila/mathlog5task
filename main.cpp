#include <iostream>
#include <string>
#include <algorithm>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>
#include <vector>
#include <queue>
#include <sstream>
#include <fstream>
#include "proof.h"




int main() {
    proof proof1;
    proof1.check_proofment();
    parser parser1 ;
    /*axiom_formal axiom_formal1;
    std::string str ;
    std::getline(std::cin , str) ;
    auto test = parser1.parse(str);
    std::cout << axiom_formal1.is_scheme(test)<<std::endl;
    std::cout << test->print();
     */
    return 0;
}